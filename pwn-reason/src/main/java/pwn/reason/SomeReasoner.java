package pwn.reason;

// TODO read http://jacopguide.osolpro.com/guideJaCoP.html
// TODO read https://jena.apache.org/documentation/ontology/

// TODO read Krisnadhi, A. et al. An Ontology Design Pattern for Chess Games.
// TODO read Smith, Barry. "Against idiosyncrasy in ontology development."
// TODO read Dennett, D. C. (1991). Real patterns.
// TODO read Smith, B. (2008). Searle and de soto: The new ontology of the social world.
// TODO read Haugeland, J. (2000). Having thought: Essays in the metaphysics of mind.

public class SomeReasoner {

  public void reason() {

  }

  /**
   * TODO.
   */
  public static void example() {
    // Store store = new Store();
    // int size = 4;
    //
    // // define finite domain variables
    // IntVar[] domainVariables = new IntVar[size];
    // for (int i = 0; i < size; i++) {
    // domainVariables[i] = new IntVar(store, "v" + i, 1, size);
    // }
    //
    // // define constraints
    // PrimitiveConstraint c1 = new XneqY(domainVariables[0], domainVariables[1]);
    // PrimitiveConstraint c2 = new XneqY(domainVariables[0], domainVariables[2]);
    // PrimitiveConstraint c3 = new XneqY(domainVariables[1], domainVariables[2]);
    // PrimitiveConstraint c4 = new XneqY(domainVariables[1], domainVariables[3]);
    // PrimitiveConstraint c5 = new XneqY(domainVariables[2], domainVariables[3]);
    //
    // store.impose(c1);
    // store.impose(c2);
    // store.impose(c3);
    // store.impose(c4);
    // store.impose(c5);
    //
    // // search for a solution and print results
    // Search<IntVar> search = new DepthFirstSearch<IntVar>();
    // SelectChoicePoint<IntVar> select =
    // new InputOrderSelect<IntVar>(store, domainVariables, new IndomainMin<>());
    //
    // boolean result = search.labeling(store, select);
    //
    // if (result) {
    // System.out.println("Solution: " + domainVariables[0] + ", " + domainVariables[1] + ", "
    // + domainVariables[2] + ", " + domainVariables[3]);
    // } else {
    // System.out.println("*** No");
    // }

    // Rcc8 rcc = new Rcc8();
  }

}
